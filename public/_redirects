# 说明：本应用是纯静态前端 + 数据快照，没有服务端路由，因此不需要 SPA 重写规则。
#
# 注意：不要为根路径添加 ` /  /index.html  200` 这类规则——
# Cloudflare 会判定为「无限循环重定向」并拒绝部署（错误码 100324），
# 因为该规则会不断把 `/` 重写回 `/index.html` 再触发自身。
# 静态托管本身已能正确返回 index.html，无需额外配置。
